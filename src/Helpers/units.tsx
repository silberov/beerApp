export const unitsToLetters = (unit:string) => {
    switch (unit) {
      case 'kilograms':
        return 'Kg';
      case 'litres':
          return 'L';
      case 'grams':
        return 'g'
        case 'celsius':
          return '°C'
      default:
        return 
    }
  }